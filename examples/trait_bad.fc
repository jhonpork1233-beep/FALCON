// Trait conformance test — INVALID: missing a required method
import string;

trait Printable {
    func show(self);
    func display(self);
}

struct Point {
    x: i64,
    y: i64,
}

// This impl is MISSING the "display" method — should fail compile
impl Printable for Point {
    func show(self) {
        println("Point shown");
    }
}

func main() {
    let p = Point { x: 1, y: 2 };
    println("Test");
}
