// Falcon benchmark: compute-bound loop
import string;
func main() {
    let mut sum: i64 = 0;
    let mut i: i64 = 0;
    while i < 100000000 {
        sum = sum + i;
        i = i + 1;
    }
    print_int(sum);
}
